#include<stdio.h>
#include<stdlib.h>

int main(void){
    int n =1,m=16;
    char cnum3[m];
    char cnum1[n];
    char cnum2[m];
    // scanf("%s",cnum3);
    scanf("%c",&cnum1[0]);
    scanf("%s",cnum2);
    // char *cnum1 = malloc(n*sizeof(char));  scanf("%s",cnum1);
    // char *cnum2 = malloc(m*sizeof(char));   scanf("%s",cnum2);
    printf("%d",cnum1[0]);
    return 0;
}
